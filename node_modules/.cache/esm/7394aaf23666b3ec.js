let Joi;_366‍.x([["validateTicketTypeData",()=>validateTicketTypeData],["validateTicketTypeUpdateData",()=>validateTicketTypeUpdateData]]);_366‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);

const ticketTypeSchema = Joi.object({
    name: Joi.string().min(2).max(50).required(),
    price: Joi.number().required(),
});

const ticketTypeUpdateSchema = Joi.object({
    name: Joi.string().min(2).max(50),
    price: Joi.number(),
});

       const validateTicketTypeData = function (req, res, next) {
    let {error, value} = ticketTypeSchema.validate(req.body);
    if(error) {
        return res.status(400).json({
            message: error.details[0].message,
        })
    }
    req.body = value;
    next();
};

       const validateTicketTypeUpdateData = function (req, res, next) {
    let { error, value } = ticketTypeUpdateSchema.validate(req.body);
    if (error) {
      return res.status(400).json({
        message: error.details[0].message,
      });
    }
    req.body = value;
    next();
  };
  

